# JSON配置文件修改工具 - 项目结构说明

## 📁 项目目录结构

```
JsonEditTool/
├── src/                          # 源代码目录
│   ├── __init__.py              # 包初始化文件
│   ├── main.py                  # 主程序入口（命令行接口）
│   ├── json_editor.py           # JSON编辑器核心类
│   ├── logger.py                # 日志管理模块
│   └── validator.py             # 参数验证模块
│
├── tests/                       # 测试目录
│   ├── __init__.py              # 测试包初始化
│   ├── test_json_editor.py      # JSONEditor单元测试
│   └── sample_config.json       # 示例配置文件
│
├── examples/                    # 示例脚本目录
│   ├── example_single.bat       # 单文件修改示例
│   ├── example_multi_files.bat  # 多文件批量修改示例
│   ├── example_add_delete.bat   # 添加和删除操作示例
│   └── example_cross_project.bat # 跨项目配置同步示例
│
├── logs/                        # 日志文件目录（运行时生成）
│   └── json_edit_tool_YYYYMMDD.log
│
├── build/                       # 打包临时目录（打包时生成）
├── dist/                        # 打包输出目录（打包时生成）
│   └── JsonEditTool.exe         # 最终可执行文件
│
├── requirements.txt             # Python依赖清单
├── build.bat                    # Windows打包脚本
├── README.md                    # 项目使用说明
├── 设计文档.md                  # 详细设计文档
├── 工作量评估.md                # 工作量评估文档
├── 项目结构说明.md              # 本文档
└── .gitignore                   # Git忽略文件配置
```

## 📦 核心模块说明

### 1. main.py - 主程序入口

**功能**：
- 命令行参数解析
- 参数验证
- 操作调度
- 错误处理和返回码管理

**关键函数**：
- `parse_arguments()`: 解析命令行参数
- `validate_params()`: 验证参数合法性
- `execute_operation()`: 执行具体操作
- `main()`: 主函数入口

**错误代码**：
```python
ERROR_CODE = {
    'SUCCESS': 0,              # 成功
    'FILE_NOT_FOUND': 1,       # 文件不存在
    'JSON_FORMAT_ERROR': 2,    # JSON格式错误
    'ENCODING_ERROR': 3,       # 编码错误
    'KEY_NOT_FOUND': 4,        # Key不存在
    'INVALID_PARAMS': 5,       # 参数验证失败
    'PERMISSION_ERROR': 6,     # 权限不足
    'KEY_EXISTS': 7,           # Key已存在
    'INVALID_PATH': 8,         # 无效路径
    'DRIVE_NOT_EXIST': 9,      # 盘符不存在
    'NETWORK_PATH_ERROR': 10,  # 网络路径错误
    'UNKNOWN_ERROR': 99        # 未知错误
}
```

### 2. json_editor.py - JSON编辑器核心

**功能**：
- JSON文件读取和保存
- 配置项查找（支持点分路径）
- 配置项增删改操作
- 自动编码检测
- 备份功能

**核心类 - JSONEditor**：
```python
class JSONEditor:
    def __init__(file_path, encoding='utf-8', indent=4, create_backup=False)
    def load() -> Dict                    # 加载JSON文件
    def save(data=None) -> bool           # 保存JSON文件
    def find_item(key_path) -> Tuple      # 查找配置项
    def update_item(key_path, value, comment=None) -> bool  # 更新配置项
    def add_item(key_path, value, comment=None) -> bool     # 添加配置项
    def delete_item(key_path) -> bool     # 删除配置项
    def get_value(key_path) -> Any        # 获取配置值
```

**特性**：
- 支持嵌套路径（如：`server.port`, `database.pool.maxSize`）
- 标准配置格式：`{"key": "xxx", "value": xxx, "_comment": "xxx"}`
- 自动编码检测（utf-8 → utf-8-sig → gbk → gb2312 → latin-1）
- 可选备份功能（带时间戳）

### 3. logger.py - 日志管理

**功能**：
- 统一日志记录
- 日志轮转（按天）
- 同时输出到控制台和文件
- 单例模式

**核心类 - Logger**：
```python
class Logger:
    def __init__(log_dir='logs', log_level=logging.INFO)
    def info(message)      # 记录INFO级别日志
    def warning(message)   # 记录WARNING级别日志
    def error(message)     # 记录ERROR级别日志
    def debug(message)     # 记录DEBUG级别日志
    def exception(message) # 记录异常（含堆栈）
```

**日志格式**：
```
2026-01-28 10:30:15 - INFO - 开始处理: update config.json server.port
2026-01-28 10:30:15 - INFO - 规范化路径: config.json -> D:\App\config.json
2026-01-28 10:30:15 - INFO - 成功加载配置文件: D:\App\config.json
```

**特性**：
- 日志文件按日期命名：`json_edit_tool_20260128.log`
- 保留最近30天日志
- 单例模式，全局唯一实例

### 4. validator.py - 参数验证

**功能**：
- 操作类型验证
- 文件路径验证和规范化
- 配置项key验证
- 值类型推断
- 编码格式验证

**核心类 - Validator**：
```python
class Validator:
    @staticmethod
    def validate_operation(operation) -> Tuple[bool, str]
    @staticmethod
    def validate_file_path(file_path, must_exist=False) -> Tuple[bool, str]
    @staticmethod
    def normalize_path(file_path) -> str
    @staticmethod
    def validate_key(key) -> Tuple[bool, str]
    @staticmethod
    def validate_value(value) -> Tuple[bool, str]
    @staticmethod
    def infer_value_type(value_str) -> Any
    @staticmethod
    def validate_encoding(encoding) -> Tuple[bool, str]
```

**路径处理特性**：
- 支持绝对路径、相对路径、网络路径（UNC）
- 环境变量展开（`%APPDATA%`, `~`）
- 路径规范化（统一分隔符）
- 路径安全检查（防止路径遍历）
- 盘符存在性检查（Windows）

**类型推断规则**：
```python
"null"/"none" → None
"true"/"false" → Boolean
"123" → int
"123.45" → float
"其他" → string
```

## 🔧 开发环境设置

### 1. 安装依赖

```bash
# 安装开发依赖
pip install -r requirements.txt

# 或仅安装打包工具
pip install pyinstaller
```

### 2. 运行测试

```bash
# 运行单元测试
python -m pytest tests/

# 查看测试覆盖率
python -m pytest tests/ --cov=src --cov-report=html
```

### 3. 开发调试

```bash
# 直接运行Python脚本
python src\main.py --help
python src\main.py update tests\sample_config.json server.port --value 8080
```

### 4. 打包成exe

```bash
# 执行打包脚本
build.bat

# 或手动打包
pyinstaller --onefile --name JsonEditTool --console src\main.py

# 生成的exe位于
dist\JsonEditTool.exe
```

## 📝 使用流程

### 开发使用流程

```
1. 编辑源代码
   ↓
2. 运行单元测试
   ↓
3. 本地测试功能
   python src\main.py ...
   ↓
4. 确认无误后提交
```

### 打包发布流程

```
1. 确保代码测试通过
   ↓
2. 执行打包脚本
   build.bat
   ↓
3. 测试生成的exe
   dist\JsonEditTool.exe --version
   dist\JsonEditTool.exe update test.json ...
   ↓
4. 发布exe文件
```

### 最终用户使用流程

```
1. 获取JsonEditTool.exe
   ↓
2. 编写BAT脚本
   @echo off
   JsonEditTool.exe update config.json server.port --value 8080
   ↓
3. 运行BAT脚本
   ↓
4. 查看日志（如有问题）
   logs\json_edit_tool_YYYYMMDD.log
```

## 🎨 设计模式

### 1. 单例模式（Logger）

确保全局只有一个Logger实例，统一管理日志输出。

```python
class Logger:
    _instance = None
    
    def __new__(cls, *args, **kwargs):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
```

### 2. 门面模式（main.py）

main.py作为统一入口，封装了所有复杂的内部逻辑，提供简单的命令行接口。

### 3. 策略模式（操作类型）

通过operation参数选择不同的操作策略（update/add/delete）。

```python
if args.operation == 'update':
    editor.update_item(...)
elif args.operation == 'add':
    editor.add_item(...)
elif args.operation == 'delete':
    editor.delete_item(...)
```

## 🔒 安全考虑

### 1. 路径安全

- 路径规范化处理
- 检查路径遍历攻击（`..`）
- 验证盘符存在性
- 检查文件权限

### 2. 数据安全

- 可选备份功能
- 原子性操作（先读后写）
- 异常回滚机制

### 3. 日志安全

- 敏感信息脱敏
- 日志文件权限控制
- 日志轮转限制大小

## 📊 性能特性

### 1. 性能目标

| 文件大小 | 处理时间 |
|---------|---------|
| < 1MB   | < 100ms |
| 1-10MB  | < 1s    |
| 10-50MB | < 5s    |

### 2. 优化措施

- 使用Python标准库（无额外依赖）
- 一次性读写（避免频繁IO）
- 路径缓存和规范化
- 单例Logger减少初始化

## 🐛 调试技巧

### 1. 查看详细日志

```bash
# 日志位于程序运行目录下
logs\json_edit_tool_20260128.log
```

### 2. 开启调试模式

修改logger.py中的日志级别：

```python
logger = get_logger(log_dir='logs', log_level=logging.DEBUG)
```

### 3. 使用--backup参数

修改前自动备份，便于回滚：

```bash
JsonEditTool.exe update config.json key --value newvalue --backup
```

### 4. 返回码检查

在BAT脚本中检查返回码：

```batch
JsonEditTool.exe update config.json key --value value
if %ERRORLEVEL% NEQ 0 (
    echo 操作失败，错误代码: %ERRORLEVEL%
    type logs\json_edit_tool_*.log
)
```

## 📚 扩展开发

### 添加新功能

1. **添加新的验证规则** - 修改`validator.py`
2. **添加新的操作类型** - 修改`main.py`的operation处理
3. **支持新的配置格式** - 修改`json_editor.py`
4. **添加新的日志级别** - 修改`logger.py`

### 示例：添加批量操作

```python
# 在json_editor.py中添加
def batch_update(self, items: Dict[str, Any]) -> bool:
    for key_path, value in items.items():
        self.update_item(key_path, value)
    return True
```

## 🔄 版本历史

### v1.0.0 (2026-01-28)
- ✨ 初始版本发布
- ✅ 支持update/add/delete操作
- ✅ 支持多种路径类型
- ✅ 完整的日志记录
- ✅ 打包成独立exe

## 📞 技术支持

遇到问题时：
1. 查看日志文件：`logs\json_edit_tool_YYYYMMDD.log`
2. 运行`JsonEditTool.exe --help`查看帮助
3. 参考README.md和设计文档
4. 查看示例脚本（examples目录）

---

**文档版本**: v1.0  
**最后更新**: 2026-01-28  
**维护者**: JsonEditTool Team
