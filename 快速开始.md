# JSON配置文件修改工具 - 快速开始指南

## 🚀 5分钟快速上手

### 步骤1：获取工具

**方式A：使用预编译的exe（推荐）**
```bash
# 已打包好的工具位于
dist\JsonEditTool.exe
```

**方式B：从源码运行**
```bash
# 确保已安装Python 3.8+
python src\main.py --help
```

### 步骤2：准备配置文件

创建一个测试配置文件 `test_config.json`：

```json
{
  "server": {
    "port": {
      "key": "port",
      "value": 8080,
      "_comment": "服务器端口"
    }
  }
}
```

### 步骤3：尝试修改配置

```bash
# 方式A：使用exe
JsonEditTool.exe update test_config.json server.port --value 9000

# 方式B：使用Python
python src\main.py update test_config.json server.port --value 9000
```

### 步骤4：验证结果

打开 `test_config.json`，你会看到端口已改为9000。

✅ 恭喜！你已经成功使用了工具。

---

## 📋 常用操作示例

### 1. 修改配置（最常用）

```bash
# 修改服务器端口
JsonEditTool.exe update config.json server.port --value 8080 --comment "Web服务端口"

# 修改数据库地址
JsonEditTool.exe update config.json database.host --value "192.168.1.100"

# 修改布尔值
JsonEditTool.exe update config.json cache.enabled --value true
```

### 2. 添加新配置

```bash
# 添加简单配置
JsonEditTool.exe add config.json server.timeout --value 30 --comment "超时时间"

# 添加嵌套配置
JsonEditTool.exe add config.json logging.level --value "INFO" --comment "日志级别"
```

### 3. 删除配置

```bash
# 删除不需要的配置
JsonEditTool.exe delete config.json server.debug

# 删除嵌套配置
JsonEditTool.exe delete config.json temp.cache.data
```

---

## 🔧 在BAT脚本中使用

### 单行命令

```batch
@echo off
JsonEditTool.exe update D:\App\config.json server.port --value 8080
```

### 带错误检查

```batch
@echo off
JsonEditTool.exe update config.json server.port --value 8080

if %ERRORLEVEL% EQU 0 (
    echo ✓ 修改成功
) else (
    echo ✗ 修改失败，错误代码: %ERRORLEVEL%
    exit /b 1
)
```

### 批量修改

```batch
@echo off
echo 开始批量修改...

JsonEditTool.exe update config.json server.port --value 8080
JsonEditTool.exe update config.json database.host --value "localhost"
JsonEditTool.exe update config.json cache.ttl --value 3600

echo 批量修改完成！
```

---

## 🌟 高级用法

### 1. 使用绝对路径

```bash
# Windows绝对路径
JsonEditTool.exe update "D:\MyApp\config.json" server.port --value 8080

# 路径包含空格
JsonEditTool.exe update "C:\Program Files\MyApp\config.json" key --value value
```

### 2. 使用环境变量

```bash
# 用户目录
JsonEditTool.exe update "%APPDATA%\MyApp\config.json" key --value value

# 程序数据目录
JsonEditTool.exe update "%ProgramData%\MyApp\config.json" key --value value
```

### 3. 修改网络路径配置

```bash
# UNC路径
JsonEditTool.exe update "\\server\share\config.json" key --value value
```

### 4. 创建备份

```bash
# 修改前自动备份
JsonEditTool.exe update config.json server.port --value 8080 --backup
```

### 5. 指定编码

```bash
# 使用GBK编码
JsonEditTool.exe update config.json key --value value --encoding gbk

# 使用UTF-8 BOM编码
JsonEditTool.exe update config.json key --value value --encoding utf-8-sig
```

---

## 🎯 实际应用场景

### 场景1：部署时修改配置

```batch
@echo off
REM 部署脚本 - deploy.bat

echo [1/3] 修改数据库配置...
JsonEditTool.exe update config.json database.host --value "prod-db.example.com"
JsonEditTool.exe update config.json database.port --value 3306

echo [2/3] 修改服务器配置...
JsonEditTool.exe update config.json server.port --value 80
JsonEditTool.exe update config.json server.host --value "0.0.0.0"

echo [3/3] 启用生产模式...
JsonEditTool.exe update config.json app.production --value true

echo 部署配置完成！
```

### 场景2：环境切换

```batch
@echo off
REM 切换到开发环境

JsonEditTool.exe update config.json database.host --value "localhost"
JsonEditTool.exe update config.json server.debug --value true
JsonEditTool.exe update config.json logging.level --value "DEBUG"

echo 已切换到开发环境
```

### 场景3：批量同步配置

```batch
@echo off
REM 同步配置到多个服务

set API_KEY=abc123xyz456

JsonEditTool.exe update "D:\Service1\config.json" api.key --value %API_KEY%
JsonEditTool.exe update "E:\Service2\config.json" api.key --value %API_KEY%
JsonEditTool.exe update "F:\Service3\config.json" api.key --value %API_KEY%

echo API密钥同步完成
```

---

## ⚠️ 常见问题

### Q1: 提示"文件不存在"

**答**: 检查路径是否正确，使用绝对路径或确保相对路径正确。

```bash
# 错误示例
JsonEditTool.exe update config.json key --value value

# 正确示例（使用绝对路径）
JsonEditTool.exe update "D:\App\config.json" key --value value
```

### Q2: 提示"权限不足"

**答**: 以管理员身份运行BAT脚本。

```batch
右键点击BAT脚本 → 以管理员身份运行
```

### Q3: 修改失败但不知道原因

**答**: 查看日志文件。

```bash
# 日志位于
logs\json_edit_tool_20260128.log

# 在BAT中查看日志
type logs\json_edit_tool_*.log
```

### Q4: 配置项不存在

**答**: 对于update操作，配置项必须已存在。如果不存在，请使用add操作。

```bash
# 如果不确定是否存在，可以先尝试add，失败后再update
JsonEditTool.exe add config.json new.key --value "value"
```

### Q5: 中文乱码问题

**答**: 指定正确的编码格式。

```bash
# 尝试GBK编码
JsonEditTool.exe update config.json key --value "中文值" --encoding gbk

# 或使用UTF-8 BOM
JsonEditTool.exe update config.json key --value "中文值" --encoding utf-8-sig
```

---

## 📚 下一步

- 📖 阅读完整的[README.md](README.md)了解所有功能
- 📝 查看[设计文档.md](设计文档.md)了解技术细节
- 💼 查看[examples](examples/)目录的更多示例
- 🧪 运行[tests](tests/)目录的测试用例

---

## 🎓 最佳实践

### 1. 总是检查返回码

```batch
JsonEditTool.exe update config.json key --value value
if %ERRORLEVEL% NEQ 0 exit /b 1
```

### 2. 重要修改使用备份

```batch
JsonEditTool.exe update config.json key --value value --backup
```

### 3. 使用绝对路径避免歧义

```batch
set CONFIG=D:\App\config.json
JsonEditTool.exe update "%CONFIG%" key --value value
```

### 4. 添加注释说明修改原因

```batch
JsonEditTool.exe update config.json key --value value --comment "修改原因说明"
```

### 5. 在脚本开头显示配置信息

```batch
@echo off
echo ================================
echo 配置修改脚本
echo 目标文件: %CONFIG%
echo 修改时间: %DATE% %TIME%
echo ================================
```

---

**🎉 开始使用吧！如有问题，请查看日志文件或文档。**
