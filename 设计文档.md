# JSON配置文件修改工具 - 设计文档

## 1. 项目概述

### 1.1 项目目标
开发一个Windows平台下的JSON配置文件修改工具，支持通过BAT脚本调用，实现配置项的增删改操作。

### 1.2 核心功能
- **支持任意路径的JSON配置文件**：通过BAT脚本传递绝对或相对路径
- **灵活的文件定位**：可操作系统任意位置的JSON配置文件
- 通过命令行参数接收操作指令
- 支持修改、添加、删除三种操作
- 同时处理key、value、_comment三个属性
- 完善的错误日志记录机制
- 打包为独立的Windows可执行文件

## 2. 技术方案

### 2.1 技术栈选型
- **开发语言**: Python 3.8+
- **JSON处理**: json模块（标准库）
- **日志记录**: logging模块（标准库）
- **命令行参数解析**: argparse模块（标准库）
- **打包工具**: PyInstaller

**选型理由**:
- Python标准库功能完善，无需额外依赖
- PyInstaller可打包成单个exe文件，便于分发
- 跨平台兼容性好，后续可扩展到其他平台

### 2.2 架构设计

```
JsonEditTool/
├── src/
│   ├── json_editor.py      # 核心编辑逻辑
│   ├── logger.py            # 日志管理模块
│   ├── validator.py         # 参数验证模块
│   └── main.py              # 主程序入口
├── tests/
│   ├── test_json_editor.py  # 单元测试
│   └── sample_config.json   # 测试用配置文件
├── build/                   # 打包输出目录
├── logs/                    # 日志目录
├── requirements.txt         # 依赖清单
├── build.bat                # 打包脚本
└── README.md                # 使用说明
```

## 3. 详细设计

### 3.1 命令行接口设计

#### 3.1.1 基本语法
```bash
JsonEditTool.exe <操作类型> <JSON文件路径> <key> [选项]
```

#### 3.1.2 参数说明

| 参数 | 说明 | 必需 | 示例 |
|------|------|------|------|
| `操作类型` | update/add/delete | 是 | update |
| `JSON文件路径` | 目标配置文件的完整路径（绝对/相对） | 是 | D:\config\app.json 或 config.json |
| `key` | 配置项的键名 | 是 | server.port |
| `--value` | 配置项的值 | 条件 | 8080 |
| `--comment` | 配置项的注释 | 否 | "服务器端口" |
| `--encoding` | 文件编码 | 否 | utf-8 |
| `--indent` | JSON缩进空格数 | 否 | 4 |

**路径说明**：
- **绝对路径**：如 `D:\App\Config\server.json`、`C:\Program Files\MyApp\config.json`
- **相对路径**：如 `config.json`、`.\configs\app.json`、`..\shared\config.json`
- **网络路径**：如 `\\server\share\config.json`（需要网络访问权限）
- **路径包含空格**：需要用双引号包裹，如 `"D:\My App\config.json"`

#### 3.1.3 操作类型说明

**1. 修改操作 (update)**
```bash
# 绝对路径示例
JsonEditTool.exe update "D:\App\config.json" server.port --value 8080 --comment "更新后的端口"

# 相对路径示例
JsonEditTool.exe update .\configs\app.json server.host --value "0.0.0.0" --comment "监听地址"
```
- 查找指定key的配置项
- 更新value和_comment字段
- 如果key不存在，返回错误

**2. 添加操作 (add)**
```bash
# 绝对路径示例
JsonEditTool.exe add "C:\MyApp\settings.json" server.timeout --value 30 --comment "超时时间(秒)"

# 相对路径示例
JsonEditTool.exe add ..\shared\config.json cache.enabled --value true --comment "启用缓存"
```
- 添加新的配置项
- 如果key已存在，返回错误或提示是否覆盖

**3. 删除操作 (delete)**
```bash
# 绝对路径示例
JsonEditTool.exe delete "D:\Projects\WebApp\config.json" server.debug

# 网络路径示例
JsonEditTool.exe delete "\\fileserver\configs\prod.json" temp.data
```
- 删除指定key的配置项
- 如果key不存在，返回警告

### 3.2 JSON数据结构设计

#### 3.2.1 标准配置项结构
```json
{
  "server": {
    "port": {
      "key": "port",
      "value": 8080,
      "_comment": "服务器监听端口"
    },
    "host": {
      "key": "host",
      "value": "localhost",
      "_comment": "服务器地址"
    }
  },
  "database": {
    "connection": {
      "key": "connection",
      "value": "mysql://localhost:3306/mydb",
      "_comment": "数据库连接字符串"
    }
  }
}
```

#### 3.2.2 支持嵌套路径
- 使用点号分隔的路径: `server.port`
- 支持多层嵌套: `database.pool.maxConnections`

### 3.3 核心模块设计

#### 3.3.1 JSONEditor类
```python
class JSONEditor:
    def __init__(self, file_path, encoding='utf-8', indent=4)
    def load(self) -> dict
    def save(self, data: dict) -> bool
    def find_item(self, key_path: str) -> tuple
    def update_item(self, key_path: str, value: any, comment: str = None) -> bool
    def add_item(self, key_path: str, value: any, comment: str = None) -> bool
    def delete_item(self, key_path: str) -> bool
```

**主要方法说明**:
- `load()`: 加载JSON文件，处理编码问题
- `save()`: 保存修改后的JSON，保持格式化
- `find_item()`: 根据点分路径查找配置项
- `update_item()`: 更新已存在的配置项
- `add_item()`: 添加新配置项
- `delete_item()`: 删除配置项

#### 3.3.2 Logger日志模块
```python
class Logger:
    def __init__(self, log_dir='logs')
    def info(self, message: str)
    def warning(self, message: str)
    def error(self, message: str)
    def exception(self, message: str)
```

**日志特性**:
- 日志文件命名: `json_edit_tool_YYYYMMDD.log`
- 支持日志轮转，每天一个新文件
- 同时输出到控制台和文件
- 错误级别自动记录堆栈信息

#### 3.3.3 Validator验证模块
```python
class Validator:
    @staticmethod
    def validate_file_path(path: str) -> bool
    @staticmethod
    def normalize_path(path: str) -> str
    @staticmethod
    def validate_operation(operation: str) -> bool
    @staticmethod
    def validate_key(key: str) -> bool
    @staticmethod
    def validate_value(value: str) -> any
```

**验证内容**:
- **文件路径合法性检查**：支持绝对路径、相对路径、UNC网络路径
- **路径规范化**：将相对路径转换为绝对路径
- **路径安全检查**：防止路径遍历攻击（如`..\..\system32\config`）
- 文件是否存在（对于update/delete）
- 文件是否可读写
- 操作类型合法性
- Key格式验证
- Value类型自动推断（数字、布尔、字符串、null）

**路径处理特性**：
- 自动展开环境变量（如`%APPDATA%\config.json`）
- 支持用户目录符号（如`~\config.json`）
- 处理路径中的空格和特殊字符
- 验证盘符存在性（Windows）

### 3.4 BAT脚本调用示例

#### 3.4.1 单文件修改（绝对路径）
```batch
@echo off
set TOOL=D:\Tools\JsonEditTool.exe
set CONFIG=D:\App\config.json

REM 修改端口号
%TOOL% update "%CONFIG%" server.port --value 8080 --comment "生产环境端口"

if %ERRORLEVEL% NEQ 0 (
    echo 修改失败，请检查日志
    exit /b 1
)

echo 配置修改成功
```

#### 3.4.2 多文件多位置修改
```batch
@echo off
setlocal enabledelayedexpansion

set TOOL=D:\Tools\JsonEditTool.exe

REM 定义多个配置文件路径
set "CONFIG1=D:\WebApp\config.json"
set "CONFIG2=C:\Services\API\settings.json"
set "CONFIG3=\\FileServer\Shared\prod.json"

echo 开始批量修改配置...

REM 修改Web应用配置
echo [1/3] 修改Web应用配置...
%TOOL% update "%CONFIG1%" server.port --value 8080 --comment "Web端口"
if !ERRORLEVEL! EQU 0 (echo    成功) else (echo    失败)

REM 修改API服务配置
echo [2/3] 修改API服务配置...
%TOOL% update "%CONFIG2%" api.endpoint --value "https://api.example.com" --comment "API地址"
if !ERRORLEVEL! EQU 0 (echo    成功) else (echo    失败)

REM 修改生产环境配置
echo [3/3] 修改生产环境配置...
%TOOL% update "%CONFIG3%" database.host --value "prod-db.local" --comment "生产数据库"
if !ERRORLEVEL! EQU 0 (echo    成功) else (echo    失败)

echo 批量修改完成
pause
```

#### 3.4.3 基于环境变量的动态路径
```batch
@echo off
set TOOL=JsonEditTool.exe

REM 使用环境变量定位配置文件
set APP_CONFIG=%APPDATA%\MyApp\config.json
set SYSTEM_CONFIG=%ProgramData%\MyApp\system.json

echo 修改用户配置...
%TOOL% update "%APP_CONFIG%" user.theme --value "dark" --comment "深色主题"

echo 修改系统配置...
%TOOL% update "%SYSTEM_CONFIG%" system.language --value "zh-CN" --comment "简体中文"

echo 配置更新完成
```

#### 3.4.4 相对路径批量操作
```batch
@echo off
setlocal enabledelayedexpansion

set TOOL=JsonEditTool.exe

REM 相对路径配置列表
set "configs[0]=.\configs\dev.json server.port 8080"
set "configs[1]=.\configs\test.json server.port 8081"
set "configs[2]=.\configs\prod.json server.port 80"

for /L %%i in (0,1,2) do (
    for /F "tokens=1,2,3 delims= " %%a in ("!configs[%%i]!") do (
        echo 正在修改: %%a - %%b
        %TOOL% update "%%a" %%b --value %%c --comment "更新端口配置"
        
        if !ERRORLEVEL! NEQ 0 (
            echo [错误] 修改失败: %%a
        ) else (
            echo [成功] 修改完成: %%a
        )
    )
)

echo 批量修改完成
pause
```

#### 3.4.5 跨项目配置同步脚本
```batch
@echo off
setlocal enabledelayedexpansion

set TOOL=D:\Tools\JsonEditTool.exe

REM 定义多个项目的配置文件路径
set "PROJECT_A=D:\Projects\ProjectA\config.json"
set "PROJECT_B=E:\Work\ProjectB\settings.json"
set "PROJECT_C=C:\Dev\ProjectC\app.json"

REM 需要同步的配置值
set DB_HOST=192.168.1.100
set DB_PORT=3306
set API_KEY=abc123xyz

echo 开始同步配置到所有项目...

REM 同步数据库配置
for %%P in ("%PROJECT_A%" "%PROJECT_B%" "%PROJECT_C%") do (
    echo 正在更新: %%~P
    %TOOL% update %%P database.host --value %DB_HOST% --comment "数据库地址"
    %TOOL% update %%P database.port --value %DB_PORT% --comment "数据库端口"
    %TOOL% update %%P api.key --value %API_KEY% --comment "API密钥"
    echo.
)

echo 配置同步完成！
pause
```

### 3.5 错误处理与日志

#### 3.5.1 错误类型定义
| 错误代码 | 描述 | 处理方式 |
|---------|------|---------|
| 1 | 文件不存在 | 记录错误日志，提示完整路径，退出 |
| 2 | JSON格式错误 | 记录错误详情，退出 |
| 3 | 编码错误 | 尝试其他编码，记录警告 |
| 4 | Key不存在 | 记录错误，提示用户 |
| 5 | 参数验证失败 | 显示使用帮助 |
| 6 | 权限不足 | 记录错误，提示管理员权限 |
| 7 | 添加已存在的key | 提示冲突，询问是否覆盖 |
| 8 | 无效路径 | 路径格式错误或包含非法字符 |
| 9 | 盘符不存在 | Windows盘符不可访问 |
| 10 | 网络路径不可达 | UNC路径无法访问 |

#### 3.5.2 日志格式
```
2026-01-28 10:30:15,123 - INFO - 开始处理: update D:\App\config.json server.port
2026-01-28 10:30:15,134 - INFO - 路径规范化: D:\App\config.json -> D:\App\config.json
2026-01-28 10:30:15,145 - INFO - 成功加载配置文件: D:\App\config.json
2026-01-28 10:30:15,167 - INFO - 找到配置项: server.port
2026-01-28 10:30:15,189 - INFO - 更新值: 8080 -> 9000
2026-01-28 10:30:15,210 - INFO - 更新注释: "服务器端口" -> "新的端口配置"
2026-01-28 10:30:15,234 - INFO - 保存配置文件成功: D:\App\config.json
2026-01-28 10:30:15,250 - INFO - 操作完成: update server.port
```

#### 3.5.3 日志存储
- **位置**: 程序运行目录下的`logs`文件夹
- **命名**: `json_edit_tool_20260128.log`
- **轮转**: 每天生成新文件，保留最近30天
- **大小限制**: 单个日志文件最大10MB

### 3.6 打包配置

#### 3.6.1 PyInstaller配置
```python
# build.spec
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['src/main.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='JsonEditTool',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='icon.ico'  # 可选：添加图标
)
```

#### 3.6.2 打包脚本 (build.bat)
```batch
@echo off
echo 开始打包 JsonEditTool...

REM 清理旧的构建文件
if exist build rmdir /s /q build
if exist dist rmdir /s /q dist

REM 执行打包
pyinstaller --onefile ^
    --name JsonEditTool ^
    --console ^
    --clean ^
    src/main.py

if %ERRORLEVEL% EQU 0 (
    echo 打包成功！可执行文件位于: dist\JsonEditTool.exe
) else (
    echo 打包失败！请检查错误信息
    exit /b 1
)

pause
```

## 4. 测试计划

### 4.1 单元测试
- JSON文件读取/写入测试
- 配置项查找测试
- 增删改操作测试
- 异常情况处理测试
- 编码兼容性测试

### 4.2 集成测试
- BAT脚本调用测试
- 多种操作组合测试
- 并发调用测试
- 大文件处理测试

### 4.3 测试用例示例

| 测试场景 | 输入 | 预期输出 | 优先级 |
|---------|------|---------|--------|
| 修改已存在的配置项 | update config.json server.port --value 8080 | 成功修改，返回0 | P0 |
| 修改不存在的配置项 | update config.json xxx.yyy --value 123 | 报错，返回4 | P0 |
| 添加新配置项 | add config.json new.key --value "test" | 成功添加，返回0 | P0 |
| 添加已存在的配置项 | add config.json server.port --value 8080 | 报错，返回7 | P1 |
| 删除已存在的配置项 | delete config.json server.port | 成功删除，返回0 | P0 |
| 删除不存在的配置项 | delete config.json xxx.yyy | 警告，返回0 | P1 |
| 处理中文注释 | update config.json key --comment "中文测试" | 正确保存中文 | P0 |
| 处理特殊字符值 | update config.json key --value "a:b;c" | 正确保存特殊字符 | P1 |
| 嵌套路径操作 | update config.json a.b.c.d --value 123 | 正确处理深层嵌套 | P1 |
| 文件不存在 | update notexist.json key --value 1 | 报错，返回1 | P0 |
| 无效JSON格式 | update invalid.json key --value 1 | 报错，返回2 | P0 |
| **绝对路径操作** | update "D:\App\config.json" key --value 1 | 成功修改，返回0 | P0 |
| **相对路径操作** | update ".\configs\app.json" key --value 1 | 成功修改，返回0 | P0 |
| **网络路径操作** | update "\\\\server\share\config.json" key --value 1 | 成功修改，返回0 | P1 |
| **路径包含空格** | update "D:\My App\config.json" key --value 1 | 成功修改，返回0 | P0 |
| **多盘符操作** | update "E:\data\config.json" key --value 1 | 成功修改，返回0 | P1 |
| **盘符不存在** | update "Z:\config.json" key --value 1 | 报错，返回9 | P1 |
| **非法路径字符** | update "D:\test<>:\config.json" key --value 1 | 报错，返回8 | P1 |
| **路径遍历攻击** | update "..\..\system32\config.json" key --value 1 | 报错（如启用安全检查），返回8 | P0 |

## 5. 使用示例

### 5.1 基本使用

#### 示例1: 修改配置（相对路径）
```batch
JsonEditTool.exe update config.json server.port --value 8080 --comment "Web服务端口"
```

#### 示例2: 添加配置（绝对路径）
```batch
JsonEditTool.exe add "D:\MyApp\config.json" database.timeout --value 30 --comment "数据库超时时间(秒)"
```

#### 示例3: 删除配置（网络路径）
```batch
JsonEditTool.exe delete "\\FileServer\Configs\prod.json" server.debug
```

### 5.2 不同路径场景

#### 示例4: 操作系统不同位置的配置文件
```batch
REM 操作程序安装目录的配置
JsonEditTool.exe update "C:\Program Files\MyApp\config.json" app.version --value "2.0.0"

REM 操作用户数据目录的配置
JsonEditTool.exe update "%APPDATA%\MyApp\settings.json" user.name --value "张三"

REM 操作公共数据目录的配置
JsonEditTool.exe update "%ProgramData%\MyApp\system.json" system.mode --value "production"

REM 操作临时目录的配置
JsonEditTool.exe update "%TEMP%\cache.json" temp.size --value 100
```

#### 示例5: 跨盘符操作
```batch
REM 修改D盘配置
JsonEditTool.exe update "D:\Projects\App1\config.json" db.host --value "192.168.1.100"

REM 修改E盘配置
JsonEditTool.exe update "E:\Backup\App2\settings.json" backup.enabled --value true

REM 修改F盘配置
JsonEditTool.exe update "F:\Shared\common.json" cache.ttl --value 3600
```

### 5.3 高级用法

#### 示例6: 指定编码
```batch
JsonEditTool.exe update "D:\App\config.json" key --value "中文值" --comment "中文注释" --encoding utf-8-sig
```

#### 示例7: 自定义缩进
```batch
JsonEditTool.exe update ".\configs\app.json" key --value 123 --indent 2
```

#### 示例8: 路径包含特殊字符
```batch
REM 路径包含空格
JsonEditTool.exe update "D:\My Application\config.json" key --value "test"

REM 路径包含中文
JsonEditTool.exe update "D:\我的应用\配置.json" key --value "测试"
```

## 6. 非功能性需求

### 6.1 性能要求
- 小文件(<1MB): 处理时间 < 100ms
- 中等文件(1-10MB): 处理时间 < 1s
- 大文件(10-50MB): 处理时间 < 5s

### 6.2 兼容性要求
- Windows 7/8/10/11 (x64)
- 无需安装Python环境
- 支持UTF-8、GBK、UTF-8-BOM等常见编码

### 6.3 可维护性要求
- 代码注释覆盖率 > 30%
- 函数/方法单一职责
- 完整的错误日志记录

### 6.4 安全性要求
- 修改前自动备份原文件（可选）
- 文件权限检查
- **路径遍历攻击防护**：防止恶意路径如`../../system32/config`
- **路径白名单机制（可选）**：限制可操作的目录范围
- **路径长度限制**：Windows路径最大260字符（可配置支持长路径）
- **特殊字符过滤**：防止路径注入攻击

## 7. 扩展功能（可选）

### 7.1 第二阶段功能
- 支持JSON数组操作
- 支持正则表达式匹配key
- 批量操作配置文件
- 配置文件diff比对
- GUI图形界面

### 7.2 第三阶段功能
- 支持YAML、TOML格式
- 配置版本管理
- 远程配置同步
- Web API接口

## 8. 交付物清单

### 8.1 开发阶段交付
- [ ] 源代码（含注释）
- [ ] 单元测试代码
- [ ] requirements.txt
- [ ] 本设计文档

### 8.2 测试阶段交付
- [ ] 测试报告
- [ ] 测试用例及结果
- [ ] Bug修复记录

### 8.3 发布阶段交付
- [ ] JsonEditTool.exe（可执行文件）
- [ ] README.md（使用说明）
- [ ] 示例BAT脚本
- [ ] 示例JSON配置文件
- [ ] 打包脚本（build.bat）

## 9. 风险评估

| 风险项 | 概率 | 影响 | 应对措施 |
|--------|------|------|---------|
| JSON格式不标准 | 高 | 中 | 增强容错性，提供格式修复建议 |
| 文件编码问题 | 中 | 中 | 支持多种编码，自动检测 |
| 并发访问冲突 | 低 | 高 | 添加文件锁机制 |
| 大文件性能问题 | 中 | 中 | 流式处理，优化内存使用 |
| 路径解析错误 | 中 | 中 | 完善路径验证逻辑 |

## 10. 后续维护计划

### 10.1 版本规划
- **v1.0**: 核心功能实现
- **v1.1**: Bug修复，性能优化
- **v1.2**: 增加备份功能
- **v2.0**: 支持批量操作和GUI界面

### 10.2 维护周期
- 紧急Bug: 24小时内响应
- 一般Bug: 1周内修复
- 功能需求: 按版本规划实施

---

**文档版本**: v1.0  
**创建日期**: 2026-01-28  
**最后更新**: 2026-01-28  
**作者**: AI Assistant
